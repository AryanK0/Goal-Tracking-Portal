{
  "name": "momentum-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "frontend": "npm --prefix frontend run dev -- --port 5173",
    "frontend:build": "npm --prefix frontend run build",
    "backend": "python -m uvicorn backend.app.main:app --host 127.0.0.1 --port 8000",
    "verify": "python -m compileall backend\\app && npm --prefix frontend run build"
  }
}
